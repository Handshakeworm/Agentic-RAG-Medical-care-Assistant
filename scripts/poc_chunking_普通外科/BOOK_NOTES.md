# 普通外科 — 本书特定笔记

通用方法论见 [`scripts/METHODOLOGY.md`](../METHODOLOGY.md)。本文件只记跟 SOP 默认值的差异。

POC 完成日期:2026-05-06。最终结果:**427 父块 / 1332 子块 / mismatch=0**。

---

## 1. 基本数据

- 出版信息:研究生规划教材(第三轮),人民卫生出版社(同胸心外科)
- 总页数:721
- 上下册:无
- TOC 字典 entries:**89**(L1=12 + L2=77)
- TOC 页范围:pg 18..19(2 页主目录)
- 正文起点:pg 20(第一章 外科总论)
- 书末截断:首个 type=title `中英文名词对照索引` 在 pg 697,丢弃 24 blocks
- 字符守恒:body kept 1,251,880 - ref dropped 405,256 = expected 846,624 ✓

---

## 2. 章节结构 — 字典 L1-L2(无篇)

| 层级 | anchor | 数量 | 备注 |
|---|---|---|---|
| L1 | 第N章 | 12 | 1..12 连续,**无篇这一层**(跟消化同) |
| L2 | 第N节 | 77 | 12/12 章有节,全连续 |
| L3 | 一、 | **0(字典外)** | 正文 type=title,Pass 1 anchor |
| L4 | (一) | **0(字典外)** | 正文 type=title,Pass 2 anchor |
| L5 | 1. | **0(字典外)** | 正文 type=title,Pass 3 anchor |

---

## 3. 跟前面书 SOP 默认值的差异

### 3.1 字典 9 本里**最干净** — strict_key 89/89 全 unique

本书节标题都是**长描述**形式(`第N节 + xxx 的历史/进展/现状/思考`),无"第一节 概述"等通用名 → strict_key 0 重复 → disambig 完全用不上 → Step 2 全 OK,**0 DISAMBIG / 0 AMBIGUOUS**。

跟其他书对比:
- 胸心外科 83/87(4 重复:跨章共享"第一节 病因认知..."等)
- 心血管 233/239(4 重复:`第一节 概述` 4 章共享)
- 内分泌 / 内科 / 神内 等 L4 字典书重复更多

### 3.2 复用胸心外科 SOP-level 修

- **`TAIL_PAGE_RE` 单 `…` 兼容**:`[…\.]+`(胸心 §3.2 升级)
- **`_detect_toc_pages` 不向外延伸**:本书 pg 18 (title='目录') + pg 19 (page_header='目录') 双 seed 已覆盖,只填充间隙(胸心 §3.1 选项)

实测:本书无单 `…` 形式,`_detect_toc_pages` 也未触发延伸边界 — 这两个修法在本书属"防御性 + 默认行为没改变",验证它们在中等体量书稳定。

### 3.3 absorb_levels = (1, 2)

字典只到 L2,不含 L3 一、 — 跟内分泌/胸心外科同。短 章/节 < 500 字跨吸收。

### 3.4 Pass 全开 — 跟胸心外科同

字典 89 entries(浅),正文 847K 字,平均每节 ~9500 字。baseline 下:
- 父块 max = 27791(第二节 腹壁疝修补手术)
- 69 个父块 > 6000

Pass 1+2+3 全开后 max 6567(剩 3 个超阈值,差额最大 567 字)。

### 3.5 BLACKLIST 加广告页

```python
BLACKLIST = {
    ..., "公众号登录 >>", "网站登录 >>", "进入中华临床影像库首页",
    "注册或登录", "临床影像库", "登录中华临床影像库步骤",
}
```

跟胸心外科同(同出版社营销资源)。

---

## 4. mineru 数据特点

### 4.1 章首识别 12/12 全 type=title

跟胸心外科一样(2024 第三轮研究生教材),**0 真兜底 / 0 CHAP_MERGED / 0 FUZZY**。继续印证 mineru 对新版规划教材的识别质量提升。

### 4.2 节末"参考文献" + "推荐阅读"占比 32%(405K 字)

比胸心外科(204K / 24%)高,本书每章末有 "参考文献" + "推荐阅读" 双标记,丢弃比例较大。RE_REF_MARKER 直接处理。

### 4.3 节标题 mineru 识别情况

77 节中 mineru 识别为 type=title 的全部 77 个。type=paragraph 形式(在 TOC 里)只是 TOC 内的版式。正文区每节首都是干净的 type=title。

---

## 5. Step 1 字典 4 类硬校验

| # | 项 | 结果 |
|---|---|---|
| 1 | 基本计数 | L1=12 + L2=77 = 89 ✓ |
| 2 | unmatched / blacklist | unmatched=**0** / blacklist 2 次(目录页眉 / 索引尾页) |
| 3 | 章序连续 | 1..12 ✓ |
| 4 | 每章节序连续 | 12/12 章有节,全连续 ✓ |
| 5 | strict_key 唯一性 | **89/89 全 unique,0 重复** ✓(9 本最干净) |

---

## 6. Step 2 全量校验

| 项 | 结果 |
|---|---|
| L1 章 12/12 | 全 AS_IS,**0 真兜底 / 0 CHAP_MERGED** ✓ |
| L2 节 77/77 | 全 AS_IS ✓ |
| Status | **101 OK / 0 DISAMBIG / 0 AMBIGUOUS**(strict_key 0 重复,disambig 用不上) |
| Action | 89 AS_IS + 12 PAGE_HEADER_FB(冗余) |
| Missing | 0 |

---

## 7. Step 3 切分结果

```
PARENT_SPLIT_THRESHOLD: 6000  (Pass 1 一、)
PARENT_REFINE_THRESHOLD: 6000 (Pass 2 (一))
PARENT_PASS3_THRESHOLD:  6000 (Pass 3 1.)
书末截断:    24 blocks
参考文献丢弃: 405256 字(每节末 "参考文献" + "推荐阅读")
父块数(节单位): 89
父块数(切):    427  (被二次切的 section: 81 个新增)
子块数:       1332

父块 size 字符:
  min=132 med=? p99=? max=6567

父块 分桶:
  [    0,       499]:     3  (0.7%)
  [  500,      1999]:   268  (62.8%)
  [ 2000,      4999]:   134  (31.4%)
  [ 5000,      9999]:    22  (5.2%)   ← 含 3 个 > 6000
  [10000,     19999]:     0  (0.0%)

子块 size 字符:
  min=132 med=610 p75=711 p90=863 p95=982 p99=1178 max=1649

子块 分桶:
  [    0,       199]:     1  (0.1%)
  [  200,       499]:   249  (18.7%)
  [  500,       999]:  1020  (76.6%)
  [ 1000,      1999]:    62  (4.7%)
```

字符守恒 mismatch=0 ✓。

### 7.1 剩 3 个 > 6000 父块都是真实数据

- `第三节 静脉血栓栓塞症` size=6567:整节纯叙事,无 一、 子标题
- `>> 二、颈动脉体瘤的临床分型` size=6328:一、 子段无 (一) 子标题
- `>> 1. 慢性胰腺炎的手术适应证` size=6070:1. 子段已是最深 anchor,无更细切点

差额最大 567 字,接受。

---

## 8. 验证性发现(无新 SOP-level)

本书是验证性 POC — 没暴露新 SOP-level 修正,只是:

1. **印证胸心外科 §3.1 / §3.2 修法稳定**(在中等体量书无副作用)
2. **印证 mineru 章首识别质量随出版年代提升**:跟胸心同 2024 第三轮研究生教材,12/12 章 type=title
3. **印证字典浅书 Pass 全开必备**:节均 ~9500 字 → 不切到 1.(6000 阈值)无法把 max 压到 6500 以下
